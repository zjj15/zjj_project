from autost.api import *

# 迁移到中控设定画面
do_segment(Segment('../../screen/迁移到中控设定画面.tcs'))
# 迁移到语言设置画面
do_segment(Segment('../../screen/迁移到语言设置画面.tcs'))
# 设定_语言设置为Chinese  (Mandarin)
do_segment(Segment('../../Action/设定_语言设置为Chinese(Mandarin).tcs'))